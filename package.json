{
  "name": "stjames-mass-planner",
  "version": "1.0.0",
  "private": true,
  "description": "St James the Apostle 6pm Mass music planner — offline HTML tool + build pipeline",
  "scripts": {
    "icons": "node scripts/generate_icons.js",
    "build": "node scripts/generate_icons.js && node scripts/make_template.js && node scripts/make_template2.js && node scripts/build-app.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": { "docx": "*", "romcal": "*" }
}
